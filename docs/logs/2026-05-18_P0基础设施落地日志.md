# 2026-05-18 P0 基础设施落地日志

- 日期：`2026-05-18`
- 负责人：`秦梓源`
- 协作方式：基础设施先落地，后续模块并行开发
- 当前状态：`🟡 进行中`

## 本次任务目标

- 把 `YouSells` 的前后端 P0 项目骨架真正落地
- 让团队成员可以按模块直接接着开发，而不是继续从零搭环境
- 同步项目文档、开发规范和基础测试基线

## 本次已完成内容

### 1. 后端基础设施

- 初始化 `yousells-server`
- 接入 `Spring Boot 3 + Spring Security + MyBatis-Plus + Redis + MySQL`
- 建立统一响应结构：`ApiResponse`、`PageResponse`、`IdResponse`
- 建立统一异常处理：`BusinessException`、`GlobalExceptionHandler`
- 建立 Session/Cookie 鉴权基线
- 补齐 `auth / dashboard / customer / followup / task / report / script` 六大业务线的 controller/service/vo/dto 占位
- 增加 P0 数据库 SQL 脚本：
  - `db/01_schema_p0.sql`
  - `db/02_seed_p0.sql`
- 增加基础接口测试：
  - `AuthControllerIntegrationTest`
  - `DashboardControllerIntegrationTest`

### 2. 前端基础设施

- 初始化 `yousells-web`
- 接入 `Vue 3 + TypeScript + Vite + Pinia + Vue Router + Element Plus`
- 建立认证 store 与登录态管理
- 建立路由名称常量、路由守卫与受保护布局
- 建立统一请求封装：`src/utils/request.ts`
- 建立模块 API 文件：
  - `auth / dashboard / customer / followup / task / report / script`
- 建立基础应用壳：
  - 侧边导航
  - 顶部栏
  - 通用页面区块组件
- 登录页接上后端登录接口
- 首页看板、客户列表、客户详情、公共安排、日报周报、话术库页面接上占位接口

### 3. 文档与流程

- 保持 `docs/07_数据库表设计.md`、`docs/08_API接口基线.md` 与当前骨架一致
- 更新根目录 `README.md`
- 补充本次开发日志

## 当前未完成项

- 后端尚未接入真实数据库实体、mapper、service 持久化逻辑
- 前端尚未接入正式表单提交流程与编辑交互
- 前后端尚未完成实际运行验证
- 自动化测试目前只覆盖登录与首页看板基线

## 当前阻塞与注意事项

- 本地 Maven 当前环境可能存在 `JDK 11 / JDK 21` 混用问题，运行前需要确认 Maven 实际使用的 Java 版本
- PowerShell 对 `npm.ps1` 有执行策略限制，前端依赖安装可能需要改用 `npm.cmd` 或 `cmd /c npm ...`
- 所有后续页面表单改动都必须安排前后端联调测试

## 下一步动作

1. 本地检查后端能否编译与跑测试
2. 本地检查前端依赖安装与构建链路
3. 基于当前骨架，分配各成员正式进入 P0 模块开发
4. 每个模块开发完成后立即补自动化测试
