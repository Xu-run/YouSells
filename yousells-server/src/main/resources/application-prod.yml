spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:mysql}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:yousells}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:yousells@prod2026}
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:yousells@redis2026}

app:
  cors:
    allowed-origins: ${WEB_ORIGINS:http://localhost,http://127.0.0.1}
  jwt:
    secret: ${JWT_SECRET:change-me-to-a-long-random-string}
